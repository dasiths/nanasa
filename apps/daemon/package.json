{
  "name": "@nanasa/daemon",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @nanasa/contracts build && vitest run",
    "smoke": "pnpm --filter @nanasa/contracts build && vitest run test/ttyd-runtime.test.ts"
  },
  "dependencies": {
    "@fastify/http-proxy": "11.6.0",
    "@fastify/static": "^10.1.2",
    "@fastify/websocket": "^11.3.0",
    "@nanasa/contracts": "workspace:*",
    "fastify": "^5.11.0",
    "ws": "8.21.1",
    "yaml": "2.9.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/ws": "8.18.1"
  }
}
